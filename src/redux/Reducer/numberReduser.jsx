import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const numberReduser = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = numberReduser.actions

export default numberReduser.reducer